# System model keys — used by dashboard-owned/system actions.
# Examples: generating agents, generating workflows, cron/system agents, and future platform automations.
# These keys are read from SYSTEM/dashboard/.env, not from shell exports like ~/.zshrc.
SYSTEM_OPENAI_API_KEY=sk-your-system-openai-key-here
SYSTEM_ANTHROPIC_API_KEY=sk-ant-your-system-anthropic-key-here
# SYSTEM_OPENROUTER_API_KEY=sk-or-your-system-openrouter-key-here
# SYSTEM_XAI_API_KEY=xai-your-system-key-here
# Legacy names are still supported if they are set in this file:
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# OPENROUTER_API_KEY=
# XAI_API_KEY=

# Optional default end-user model keys.
# If set, these are the default execution keys for logged-in users until BYOK is captured in-app.
# By default, user agents/workflows do NOT fall back to system keys unless ALLOW_SYSTEM_KEYS_FOR_USER_EXECUTION=true.
# USER_OPENAI_API_KEY=
# USER_ANTHROPIC_API_KEY=
# USER_OPENROUTER_API_KEY=
# USER_XAI_API_KEY=
# USER_NEBIUS_API_KEY=
# ALLOW_SYSTEM_KEYS_FOR_USER_EXECUTION=false

# Brokered agent-skill secrets (optional).
# Use a random value of at least 32 characters. Keep this operator key stable across restarts;
# losing or changing it makes existing encrypted workspace skill secrets unreadable.
# Generate one with: openssl rand -base64 48
# CLAWMAX_SECRET_MASTER_KEY=

# Gmail delegated OAuth (optional; required to connect Gmail accounts).
# Register the callback URI exactly as configured in Google Cloud.
# GMAIL_OAUTH_CLIENT_ID=
# GMAIL_OAUTH_CLIENT_SECRET=
# GMAIL_OAUTH_REDIRECT_URI=http://localhost:3001/api/mail/oauth/gmail/callback

# Microsoft 365 delegated OAuth (optional; required to connect Outlook accounts).
# Register the callback URI exactly as configured in Microsoft Entra.
# Tenant may be common, organizations, consumers, a tenant ID, or a verified domain.
# MICROSOFT365_OAUTH_CLIENT_ID=
# MICROSOFT365_OAUTH_CLIENT_SECRET=
# MICROSOFT365_OAUTH_REDIRECT_URI=http://localhost:3001/api/mail/oauth/microsoft365/callback
# MICROSOFT365_OAUTH_TENANT=common
# Agent Runtimes (optional) — OpenClaw is always the default agent execution
# CLI. Agents can instead be pinned to the Claude Code or Factory Droid CLI,
# per-agent (agent editor) or by enabling the CLI (Integrations > Runtime).
# CLAUDE_BIN=/path/to/claude   # Override CLI path (defaults to `which claude`, then ~/.local/bin/claude)
# DROID_BIN=/path/to/droid     # Override CLI path (defaults to `which droid`, then ~/.local/bin/droid)
# FACTORY_API_KEY=fk-your-factory-api-key-here
# Claude Code reads ANTHROPIC_API_KEY directly — set SYSTEM_ANTHROPIC_API_KEY / USER_ANTHROPIC_API_KEY above, or BYOK it in-app.
# Which CLI runtimes are enabled by default (comma-separated: claude,droid). Deployment default —
# a workspace's own Integrations > Runtime selection overrides this. Mirrors WORKSPACES_INTEGRATIONS_THIRD_PARTIES.
# WORKSPACES_INTEGRATIONS_RUNTIMES=claude,droid

# Senso (optional — shared context / knowledge layer for agents)
# Used for multimodal ingest, search, generation, and MCP-based context access.
# SENSO_API_KEY=

# Comet Opik (optional — for token metering)
OPIK_API_KEY=your-opik-key-here
OPIK_WORKSPACE="your-workspace"
OPIK_PROJECT_NAME="ClawMax.ai - dev"

# ClawMax Dashboard Environment
OPENCLAW_WORKSPACE=/path/to/your/workspace
NODE_ENV=development
DASHBOARD_PORT=3001
# Auth-bypassed dashboards are forced onto loopback unless this explicit risk override is set.
# Containers that publish the host port on loopback may need the override for their internal bind.
# DASHBOARD_HOST=127.0.0.1
# DASHBOARD_ALLOW_UNAUTHENTICATED_NETWORK_BIND=false
# Docker Compose publishes on host loopback by default; change only for an authenticated deployment.
# DASHBOARD_BIND_ADDRESS=127.0.0.1
CORS_ORIGIN=http://localhost:5173
# Optional extra template roots, separated by ':' on macOS/Linux or ';' on Windows.
# Each root may contain:
# - agents/ and organizations/ subdirectories, or
# - a flat directory of organization template folders (useful for private enterprise template repos)
# CLAWMAX_EXTRA_TEMPLATE_DIRS=/opt/clawmax-enterprise/templates
# Optional extra partner roots, comma-separated.
# Each root may contain partner folders like PARTNERS/<slug>/partner.json.
# CLAWMAX_EXTRA_PARTNER_DIRS=/opt/clawmax-partners,/opt/private-partners
# Optional local-only plugin opt-in for dormant test plugins.
# Set this in your local SYSTEM/dashboard/.env; do not commit live plugin enablement.
# CLAWMAX_ENABLED_PLUGINS=plugin-guardrails,plugin-evals
# Optional local-only plugin repo roots, separated by ':' on macOS/Linux or ';' on Windows.
# Each entry may be either:
# - a directory containing plugin folders, or
# - a standalone plugin repo root with its own clawmax-plugin.json
# CLAWMAX_PLUGIN_PATHS=/absolute/path/to/private-plugin-repo-a:/absolute/path/to/private-plugin-repo-b
# Optional local-only plugin shutdown switch for regression runs that must load zero plugins.
# CLAWMAX_DISABLE_DEFAULT_PLUGINS=true
# Control which partner integration pages appear in Workspaces Integrations.
# Example: WORKSPACES_INTEGRATIONS_THIRD_PARTIES=senso,opik,github,resend,cognee,gmail,microsoft365,digo
# WORKSPACES_INTEGRATIONS_THIRD_PARTIES=senso,opik,github,resend,cognee,gmail,microsoft365,digo
# Control whether Ollama appears in Workspaces Integrations and model-selection UI.
# Default behavior:
# - local / on-prem with dashboard .env present: enabled
# - managed / cloud runtime with no dashboard .env: hidden
# Set explicitly to true to force-enable it, or false to force-hide it.
# DASHBOARD_ENABLE_OLLAMA=false
# Default Ollama URL behavior:
# - local/native development falls back to http://localhost:11434
# - containerized/on-prem deployments should inject the reachable Ollama URL explicitly
#   (for example: http://host.containers.internal:11434)
# OLLAMA_BASE_URL=http://localhost:11434
# DASHBOARD_PUBLIC_URL=http://localhost:3001  # Optional: force OAuth callback/session origin
# DASHBOARD_APP_URL=http://localhost:5173      # Optional: force post-login/logout redirect target
# Keep auth cookies Secure by default. For an isolated plain-HTTP local fixture,
# this is honored only with DASHBOARD_DEPLOYMENT_KIND=local and a matching
# loopback/.localhost DASHBOARD_APP_URL. Never enable it for shared deployments.
# DASHBOARD_ALLOW_INSECURE_LOCAL_COOKIES=false
# DASHBOARD_TRUST_PROXY=false                 # Set true behind a trusted reverse proxy
# Generic maintenance banner contract (default-off when unset)
# MAINTENANCE_BANNER_ENABLED=false
# MAINTENANCE_BANNER_TEXT=Planned maintenance is scheduled for tonight.
# MAINTENANCE_BANNER_LEVEL=warning            # info | warning | critical
# MAINTENANCE_BANNER_START_AT=2026-04-23T01:00:00Z
# MAINTENANCE_BANNER_END_AT=2026-04-23T03:00:00Z
# MAINTENANCE_BANNER_LINK=https://status.example.com
# MAINTENANCE_BANNER_DISMISSIBLE=true

# ──────────────────────────────────────────────
# Authentication
# ──────────────────────────────────────────────
# Select dashboard auth mode:
# - github_oauth  : GitHub OAuth only
# - email_otp     : email login code only
# - bypass        : no auth (local-only)
# If omitted, the dashboard defaults to github_oauth.
# Valid values: github_oauth | email_otp | bypass
# DASHBOARD_AUTH_MODE=github_oauth

# Set BYPASS_OAUTH=true only for solo/local development when you do not need auth.
# WARNING: Do NOT enable in production or shared deployments.
BYPASS_OAUTH=true

# GitHub OAuth (required when BYPASS_OAUTH is false)
# Create at: GitHub > Settings > Developer settings > OAuth Apps
# Callback URL: http://localhost:3001/api/auth/github/callback
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
# Optional for hosted/cloud GitHub workflow execution:
# - local/on-prem can keep using `gh auth login`
# - hosted/cloud should prefer a runtime token plus a default repo in Workspaces Integrations
# GITHUB_TOKEN=ghp_xxx
# GH_TOKEN=ghp_xxx
# GITHUB_ALLOWED_USERS=yourgithublogin  # Optional: comma-separated allowed logins
# JWT_SECRET=replace-with-a-long-random-secret  # Recommended for stable session signing

# Email OTP (single-user cloud / on-prem / local dev)
# DASHBOARD_AUTH_MODE=email_otp
# OTP_ALLOWED_EMAILS=you@example.com
# OTP_EXPIRY_MINUTES=15
# OTP_FROM_EMAIL=ClawMax <onboarding@your-verified-domain.com>
# Optional fallback shared sender if OTP_FROM_EMAIL is unset:
# SIGNUP_FROM_EMAIL=ClawMax <onboarding@your-verified-domain.com>
# OTP_EMAIL_SUBJECT=Your ClawMax login code
# For explicit OTP log mode, log the OTP to the main server output instead of sending:
# Valid value: log
# Any other value, or leaving it unset, disables OTP log mode.
# OTP_DEV_MODE=log
# For production delivery via Resend:
# RESEND_API_KEY=re_xxx

# Ngrok tunnel (optional, for --ngrok flag)
# NGROK_URL=yoursubdomain.ngrok.dev
